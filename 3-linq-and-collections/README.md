# 3 — LINQ & Collections

Run:
```bash
dotnet restore
dotnet run
```

**Try:**  
- Group by score ranges (e.g., 90+, 80–89, etc.)  
- Take the top N students  
- Use `Distinct`, `GroupBy`, `ToDictionary`
